---
title: 'Divider'
description: 'A divider component. Can be used to separate content. Supports vertical orientation.'
---

import TypeDoc from '@docs/solid/components/layout/divider.md';
import { BaseColor, ContentColor } from '@packages/browser-utils';
import { Interactive } from '@src/components/interactive';

<Interactive
  componentName="Divider"
  argDefinitions={{
    color: {
      type: 'string',
      options: Object.values({ ...BaseColor, ...ContentColor }),
    },
    variant: {
      type: 'string',
      options: ['straight', 'sleek'],
    },
    vertical: {
      type: 'boolean',
    },
  }}
/>

<TypeDoc />
